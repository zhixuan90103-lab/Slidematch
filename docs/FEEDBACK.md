# 反馈 — 基础选中 / 基础消除 / 变色 / 合成

玩法对错让 [DESIGN.md](./DESIGN.md) · [ITEMS.md](./ITEMS.md)。点中尺子让 [OPERATION.md](./OPERATION.md)。下落占坑让 [DROP.md](./DROP.md)。  
数字真源：`src/game/design.ts` 的 `FEEL`。  
实现：`mount.ts` · `clearFx.ts` · `items.ts` · `drop.ts`。

本文是 **基础反馈**。阶段 D（6/8/10 档视+震）未做；音效未做。

---

## 两套语言（不要混）

| | 划线选中 | 变色标记 |
|--|--|--|
| 谁 | 手指划过的路径格 | `extraClearCells` 的锁定色散子 |
| 滑动中 | 浮起 + 放大 + Additive + 晃 | **只 Additive 淡入**，不浮 |
| 松手被选中后 | 保持浮起 | 保持已有 Additive，只再上浮（不再叠一层光） |
| 无效抬手 | 150ms 大小+位移归位 | 标记淡出 |

禁止：散消格用第二套描边/圆点；标记格被选中后再叠一层光。

---

## 基础选中

命中 `stable` 才起划。未命中无反馈。

- 放大到 **1.05**，上浮 **6px**，弹簧（刚度 480 / 阻尼 12 / 初速 7.5），选中时转速跟弹簧速度走。
- Additive `plus-lighter`，透明度 **0.18** × 选中进度。变色散消标记 **0.1**。
- 其它色透明度 **0.5**，过渡 **80ms**。
- 浮稳后待机：±**0.3px** 起伏。
- 再划已选中格：下沉弹簧再弹回（回退到队尾也会触发）。新入队第一下不叠下沉。
- 无效抬手（&lt;2）：**150ms** ease-out 缩回+落下。有效抬手：**跳过归位**，从悬浮进消除。

---

## 基础消除

只消被选中的格（路径；变色再加散消名单）。

1. 缩小 **150ms** ease-in 收到 0；贴图保持**抬手那一刻的显示**（换锁色后的新色 / 魔法贴图）。**缩的时候格子不放**（上方不落）。
2. 缩完才腾格，并在格心 **圆形发射器** 出碎屑。
3. 碎屑 **3–5** 粒，造型随机（圆/胶囊/菱形/三角/芯片），颜色=**抬手那一刻的显示色**（换锁色后的贴图色 / 魔法贴图，不是逻辑色），立刻出现，整段寿命淡出，重力下落，独立于棋子隐藏。
4. 有效抬手一记 `medium` 震。

---

## 翻牌（共用规格）

换锁色、魔法底板、魔法回翻、金币回翻 **共用这一套**。不要另写 6+6 或拆两段缩放。

| 项 | 值 |
|----|----|
| 时长 | `recolorSec` **200ms** |
| 前段 | 当前外观 `00–06`（7 帧，正面→侧棱） |
| 后段 | 目标外观 `19–23`（5 帧，侧棱→正面）再静图 |
| 缩放 | 绕图片中心，`1 → recolorScale(1.35) → 1`，峰值在侧棱（`sin(πu)`） |
| 选中 | 翻的过程中保持路径选中（1.05 / 上浮 6px），不要一边翻一边收选中 |
| 逻辑色 | 不变。只换贴图。 |

实现：`convertRecolorLook` / `convertRecolorScale`。横条 12 帧动画 + 末格静图，停稳不换 URL。帧序仍是 00–06 + 19–23。棋子出池必须换新 `id`。

有效抬手开始消：先记下当时显示色（`clearLook`，缩没和碎屑用它），再丢掉翻面状态（不反向播）。

---

## 变色子表现

规则见 [ITEMS.md](./ITEMS.md)。

**滑动中**

- 散子不浮。Additive **160ms** 淡入标记（`markGlow` 0.1）；摘名额则淡出。
- 手指上方奶油气泡，数字 = **当前路径格数**（不是散消数）。
- **换锁色：** 路径含变色、无魔法、锁色已定 → 路上普通子走「翻牌共用规格」翻成锁色。变色子自己不播。回退反向同一套。

**魔法滑动（两层同时存在）**

底板 = 白板 `magic_bai`（独立 overlay，原棋子横条不换 URL）。金币 = 独立层 `yaw-2d/coin`，停在横条末格。逻辑色不变。非魔法时 overlay 透明度 0；魔法缩没时藏原色子、只缩白板。

| 时机 | 底板 | 金币 |
|------|------|------|
| 点中/划入魔法 | 从**该魔法格**按圈扩散翻成白板（切比雪夫，每圈 50ms） | 该格翻牌开始后再晚 100ms 出场；yaw 一遍 400ms；缩放 0.4→1.2→0.9 上浮回弹 |
| 无效抬手 / 取消 / 退回路上无魔法 | **全盘一起**翻回（不按圈），300ms，共用规格 `00–06`→`19–23` | 前 7 帧内淡出；缩放跟底板 1→1.35→1 |
| 有效抬手 | **路径格**：不要一起缩。该格金币一开始飞，盘上金币立刻藏，**同时**白板才缩。无碎屑。 | 飞向 SCORE：从上到下（同行列差 10ms，行差 70ms 保证下行更晚）；同一飞行时长；浅弧；yaw 循环；缩放前 1/3 放到 1.15、后 2/3 收到 0.4 |
| | **非路径格**：全盘一起翻回原棋子（同上 300ms） | 前 7 帧淡出 |

盘上魔法子平时仍是 `piece-magic.png`。

**禁止 / 已修 bug**

- 出池必须换新 `piece.id`，否则翻面/金币层会套到新子上。
- 有效抬手只清路径格的翻面状态；非路径格要能翻回，不要 `recolorFx.clear()`。
- 缩没/碎屑用 `clearLook`（抬手那一刻的显示），魔法路径用飞金币代替碎屑。

**松手有效且有散消**

1. 气泡数字从路径长往下减，间隔 **70ms**；每减 1 选中 1 颗散子（保持标记光；停住 **1.1 / 9px**，过冲 `popVel` 24；无旋转晃动）。变色子不再轻跳。
2. **全部散子都发出选中后** 再停 **500ms**（从最后一颗开始上浮起算，不等弹簧停稳）。
3. 路径 + 散子一起消。倒数期间不接受新划。

无散消：松手立刻基础消路径。

---

## 合成道具

普通划达到变色 / 魔法门槛时，**路径**飞向队尾再出道具；散消仍走基础消除。变色与魔法同一套，魔法略长、略大（`magicMul`）。飞入的路径子不打碎屑。

### 飞入

从路径起点依次飞向**队尾格**（不是手指像素）。队尾自己缩小。

- 单颗收缩固定 `flySec`（**170ms**，与 5 格相同）。缩小与 **alpha 淡出**同一条加速曲线（`t²`），收到 `flyOpacityEnd` **0**。
- 整段总时长对齐 `refCount`=5（约 **274ms**）；多子只压缩间隔，下限 `staggerMin`。
- 飞入子显示层级高于静止子。

### 腾格与下落

- 基础消 / 散消：缩完才腾格。
- 合成飞入：最后一颗开始飞后再过 `vacateSec`（**120ms**），路径格**一起**腾格，避免一格一格漏导致下落卡顿。
- 腾格后**各列都可以落**。道具只锁**它自己那一格**：弹出未落稳时该格不接子、道具自己不掉；同列上方的空洞照常补。
- 队尾可比飞入结束提前 `spawnLead` 弹出。

### 道具弹出

队尾从 0 过冲放大（`spawnSec` **400ms**）：放大带一点转和上移（5 格上移 **8px**）；缩回原大后原地衰减晃动（转 + 缩放 + 位移过冲回位）。路径越长过冲越大（5 格倍率 1，每多 1 格 `popAmpPer`，上限 `popAmpMax`）。弹出期间该格不可划。

变色子 / 魔法子：放大到过冲峰值前抽 `yaw-2d/convert` 或 `yaw-2d/magic`（00–06、19–23）；到最大后切回静图。缩放 / 位移 / 转曲线不改（魔法略长、略大）。

---

## `FEEL` 分组

| 组 | 管 |
|----|----|
| `select` | 浮起/放大/叠光/归位/暗其它/待机/再划下沉 |
| `clear` | 缩没时长、收走上浮、散消错开 |
| `convert` | 气泡、标记淡入、倒数、换锁色 yaw / 1→放大→1 |
| `fx` | 碎屑数量/寿命/速度/重力/大小/发射半径 |
| `synth` | 飞入、腾格、弹出缩放/上移/转、路径长度倍率 |

改反馈只改 `FEEL` 与本文，不要在 `mount.ts` 写死数字。
